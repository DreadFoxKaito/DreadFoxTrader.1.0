# Core web app runtime
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
python-dotenv==1.0.1

# Broker/API clients and encrypted local credential storage
cryptography==46.0.5
httpx==0.28.1
requests==2.32.5
robin-stocks==3.4.0

# Trading script math and process management
numpy==2.4.2
psutil>=5.9,<8

# Optional feature packages live in companion files:
# - requirements-ai.txt for local semantic memory embeddings.
# - requirements-sound.txt for crypto script sound playback.
# - requirements-strategy.txt for strategy_forge --mode optuna.
#
# optuna is only needed for strategy_forge --mode optuna.
# It is intentionally unpinned to the local venv because it was not installed
# here; uncomment when that optimization mode is needed.
# optuna>=4,<6

# SQLite is provided by Python's standard library.
